{
  "name": "whatnot-mana-leaderboard",
  "version": "1.0.0",
  "private": true,
  "description": "Whatnot Fantasy Mana-Leaderboard OBS Overlay System",
  "scripts": {
    "dev": "concurrently -n \"SERVER,OVERLAY\" -c \"blue,magenta\" \"npm run dev:server\" \"npm run dev:overlay\"",
    "dev:server": "npm --prefix server run dev",
    "dev:overlay": "npm --prefix overlay run dev",
    "build": "npm --prefix overlay install && npm --prefix overlay run build && mkdir -p dist && cp -R overlay/dist/* dist/",
    "build:all": "npm --prefix server install && npm --prefix server run build && npm --prefix overlay install && npm --prefix overlay run build",
    "install:all": "npm install && npm --prefix server install && npm --prefix overlay install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
